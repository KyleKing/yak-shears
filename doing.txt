Review syncthing of database in:
claude --resume e9144381-5145-452a-b770-e3f7a05fa844

Review deployment options to push out these new changes:
claude --resume 1dab60b7-5ea4-4493-bc39-03de92f02791

1. worth doing before calling this fully verified: video upload/transcode, search, and a browser console CSP check
1. Review the observability ADR and plan next steps there

Consider identifying when a JSON file is out of date when it was loaded and a restart is necessary (eg when users change, or when setting are modified outside of the app?)

2026-07-27
- typed-jinja is no longer installed into the project venv, so its LSP extra is gone from my editor. Either publish typed-jinja or wire the editor at the sibling checkout
- ruff reports 16 errors and a formatting diff in yak_shears/_yak/handlers.py that predate today. CI never ran ruff, so nothing caught them
- consider adding ruff check and ruff format --check to CI, otherwise lint drift keeps accumulating unseen

2026-07-28 - typed-jinja could generate the render_* helpers
- the seven render_* functions in yak_shears/_templates/__init__.py (render_error, render_auth_login, render_yaks, render_new, render_yak_edit, render_settings, render_search) each restate the parameter list that their template's {#def ... #} header already declares, then call ENV.get_template(name).render(...). typed-jinja parses that same header and its generate_wrapper emits that same body, so the signatures are maintained twice and can drift from the headers with nothing catching it
- all 11 templates under yak_shears/_templates/ already carry a {#def #} header, so no template work is needed to adopt this
- what blocks a straight swap: generate_wrapper returns Markup while every helper here returns HTMLResponse, and render_error also sets an HTTPStatus. typed-jinja's env_import option already covers pointing generated code at yak_shears._templates.ENV
- helpers that derive data before rendering (sort links, palette lookups, pagination) would keep a hand-written body that calls the generated function rather than being replaced by it
- decide whether typed-jinja grows a wrap-the-render option or whether this repo keeps its helpers and generates only the inner typed render. The matching note is in typed-jinja's doing-2.txt


Notes
- Logout is too frequent?
- Buggy list that cursor jumps around to page bottom or back up, when it shouldn't
- Other?
