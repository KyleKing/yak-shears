Review syncthing of database in:
claude --resume e9144381-5145-452a-b770-e3f7a05fa844

Review deployment options to push out these new changes:
claude --resume 1dab60b7-5ea4-4493-bc39-03de92f02791

1. worth doing before calling this fully verified: video upload/transcode, search, and a browser console CSP check
1. Review the observability ADR and plan next steps there

Consider identifying when a JSON file is out of date when it was loaded and a restart is necessary (eg when users change, or when setting are modified outside of the app?)

2026-07-27
- typed-jinja is no longer installed into the project venv, so its LSP extra is gone from my editor. Either publish typed-jinja or wire the editor at the sibling checkout
- ruff reports 16 errors and a formatting diff in yak_shears/_yak/handlers.py that predate today. CI never ran ruff, so nothing caught them
- consider adding ruff check and ruff format --check to CI, otherwise lint drift keeps accumulating unseen

2026-07-28 - typed-jinja could generate the render_* helpers
- DONE 2026-08-04: migrated to the published types-for-jinja. render_auth_login, render_yaks, render_new, render_yak_edit, render_settings, and render_search are now imports from yak_shears/_templates/_generated/ (types-for-jinja wrapper, return_type = HTMLResponse, validator = beartype); render_error stays hand-written since it sets an HTTPStatus the generated function doesn't take. current_route moved from a pure typed-jinja global to a real header param on the five templates that vary it per page, since a generated wrapper only forwards its own template's header params, not config globals
- hk.pkl's typed-jinja step called a `check` subcommand that no longer exists upstream (removed; replaced by generate + your own checker + remap). Rewired to `types-for-jinja generate && types-for-jinja remap -- ty check --output-format gitlab _jinja_stubs`
- blocked: `ty check` on the generated stubs currently fails on templates using the "no line-aligned form" fallback (yak/streams.html.jinja, yaks/index.html.jinja, and base.html.jinja's nav block pulled in through others): the fallback stub references `_tj_f_default` without importing it, and emits `if (...):` followed by an unindented `pass`, both invalid. Confirmed pre-existing (reproduces with current_route reverted to a pure global), so it's a types-for-jinja bug, not a yak-shears regression. Needs a fix in the sibling types-for-jinja repo before the hk.pkl check step can pass


Notes
- Logout is too frequent?
- Buggy list that cursor jumps around to page bottom or back up, when it shouldn't
- Other?
